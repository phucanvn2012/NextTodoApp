import Layout from '../components/layout';
import TaskTable from '../components/tasktable';

const Task = (props) => (
    <div>
        <Layout>
            <TaskTable />
        </Layout>
    </div>
);
export default Task;